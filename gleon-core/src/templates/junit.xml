<?xml version="1.0" encoding="UTF-8"?>
<testsuites name="Gleon Tests" tests="{{ total_tests }}" failures="{{ failed_tests }}">
{% for tc in test_cases %}
  <testsuite name="{{ tc.name }}" tests="{{ tc.results | length }}" failures="{{ tc.failures }}">
    {% for res in tc.results %}
    <testcase name="{{ res.name }}" classname="{{ tc.name }}">
      {% if res.status != "Success" %}
      <failure message="{{ res.failure_message }}">{{ res.failure_message }}</failure>
      {% endif %}
    </testcase>
    {% endfor %}
  </testsuite>
{% endfor %}
</testsuites>
